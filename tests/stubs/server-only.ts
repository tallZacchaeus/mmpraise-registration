/**
 * Test stub for Next.js's `server-only` guard.
 *
 * That package throws when imported outside a Server Component, which would stop
 * us unit-testing server modules under Node. Aliasing it here keeps the guard
 * active in the real build while letting the tests import the modules directly.
 */
export {}
